<h4 class="ml-3">ユーザー一覧</h4>
<% @users.each do |user| %>
  <div class="container mt-5">
    <div class="row mb-2">
      <div class="col-3">
        ユーザーID
      </div>
      <div class="col-9">
        <%= user.id %>
      </div>
    </div>
    <div class="row mb-2">
      <div class="col-3">
        ユーザー名
      </div>
      <div class="col-9">
        <%=link_to admin_user_path(user.id) do%>
           <%=user.nickname%>
         <%end%>
      </div>
    </div>
    <div class="row mb-2">
      <div class="col-3">
        メールアドレス
      </div>
      <div class="col-9">
        <%= user.email %>
      </div>
    </div>
    <div class="row mb-2">
      <div class="col-3">
        ステータス
      </div>
      <div class="col-9">
        <% if user.is_deleted%>
          退会
        <%else%>
          有効
        <%end%>
      </div>
    </div>
    <div class="row mb-2">
      <div class="col-3">
        利用可否
      </div>
      <div class="col-9">
        <% if user.is_active%>
          無効
        <%else%>
          有効
        <%end%>
      </div>
    </div>
  </div>
<%end%>

<%= link_to "Q＆A一覧", admin_questions_path %>
<%= link_to "レビュー一覧",admin_reviews_path %>
<%= link_to "ハウスルール一覧",admin_rules_path %>
<%= link_to "お問い合わせ一覧",admin_contacts_path %>