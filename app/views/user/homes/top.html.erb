<%@areas.each do |area|%>
  <%=link_to area.area_name,seach_path(area_id: area.id)%> <%#左が名前(皆が見てわかりやすい名前)右が値%>
<%end%>

<%@tags.each do |tag|%>
  <%=link_to tag.name,seach_path(tag_id: tag.id)%>
<%end%>
